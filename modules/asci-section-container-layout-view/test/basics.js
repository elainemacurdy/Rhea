var test = require('tape');
var sectionContainerLayoutView = require('../asci-section-container-layout-view.js');

test('first test', function(t) {
  t.equal(1, 1, 'should make sure math is working');
  t.end();
});
